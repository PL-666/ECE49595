name: main CI

# FIXME: do we need "merge" here?
on: [push, pull_request]

jobs:
  build:
    name: UnityGame
    runs-on:
      matrix:
        # Support windows, macOS, ubuntu
        version: [windows-latest, macOS-latest, ubuntu-latest]
    steps:
      # Checkout
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          # Enable large file system (lfs)
          lfs: true
      
      # TODO: Cache

      # TODO: Test

      # TODO: Build

      # Output
      - uses: actions/upload-artifact@v2
        with:
          name: Build
          path: build

