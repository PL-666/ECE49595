name: main CI

# FIXME: do we need "merge" here?
on: [push, pull_request]

jobs:
  build:
    name: UnityGame
    # Support windows, macOS, linux
    runs-on: [self-hosted, windows-latest, macos-latest, ubuntu-latest]
    steps:
      # Checkout
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          # Enable large file system (lfs)
          lfs: true
      
      # TODO: Cache

      # TODO: Test

      # TODO: Build

      # Output
      - uses: actions/upload-artifact@v2
        with:
          name: Build
          path: build

